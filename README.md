# PIN Login Demo

Minimal Node.js Express app demonstrating login using a numeric PIN as the password.

Run:

```bash
npm install
npm start
# then open http://localhost:3000
```

Demo user: username `alice`, PIN `1234`.
# PIN Login Demo

Minimal Node.js Express app demonstrating login using a numeric PIN as the password.

Run:

```bash
npm install
npm start
# then open http://localhost:3000
```

Demo user: username `alice`, PIN `1234`.
# NewYearTracker